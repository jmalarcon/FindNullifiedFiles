<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net5.0</TargetFramework>
    <ApplicationIcon>FindNullifiedFiles.ico</ApplicationIcon>
    <Authors>José M. Alarcón - JASoft.org</Authors>
    <Description>Finds all the files having only nulls, that are inside an specified folder (or subfolders).</Description>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>

</Project>
